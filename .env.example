# Supabase connection (Vite reads these at build time via import.meta.env)
# Copy this file to ".env" and fill in your project's values. Do NOT commit ".env".
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
